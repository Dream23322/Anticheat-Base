
/**
 * @name checkOnHit
 * @description Runs checks that require a Player and an Entity that the player has hit.
 * @param {Player} player - The player who hit the entity
 * @param {Entity} entityHit - The entity that was hit
 * @returns {void}
 * @example checkOnHit(player, entityHit);
 */
export function checkOnHit(player, entityHit) {
    // Run checks
}